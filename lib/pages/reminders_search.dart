import 'package:flutter/material.dart';
import 'package:loc/main.dart';

class RemindersSearch extends StatelessWidget {
  const RemindersSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
